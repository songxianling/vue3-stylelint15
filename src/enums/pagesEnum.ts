export enum RULETYPE {
  VIP = 'vip',
  SELFINFO = 'selfInfo'
}

export enum CouponStatusEnum {
  /**未使用 */
  NOT_USED = 0,
  /**已核销 */
  USED = 1,
  /**已过期 */
  EXPIRED = 2,
  /**使用中 */
  USING = 3
}
